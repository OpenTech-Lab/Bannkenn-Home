version: '3.9'

services:
  device-monitor:
    image: toyofumi111/bannkenn-home:latest
    container_name: device-monitor
    restart: unless-stopped

    # Required for ARP scan and raw packet capture
    # Synology needs the process to run as root inside a privileged container.
    # The non-root + file capability model is not reliable there for AF_PACKET.
    network_mode: host
    privileged: true
    user: "0:0"

    environment:
      - INTERFACE=${INTERFACE:-eth0}
      - SUBNET=${SUBNET:-192.168.11.0/24}
      - PORT=8080
      - GEO_DATA_DIR=/data
      - SCAN_INTERVAL=${SCAN_INTERVAL:-30}
      - LABELS_FILE=/data/labels.json
      - RUST_LOG=device_monitor=info,warn

    volumes:
      # MMDB geo databases + labels.json (rw so labels persist)
      - ./device-monitor/data:/data

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
